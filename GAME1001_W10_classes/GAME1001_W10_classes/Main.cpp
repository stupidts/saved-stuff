#include <iostream>
#include <string>
#include "Class.h"
using namespace std;

// Constructors are member functions that are automatically called when an object of the glass is created
// Can be used to initialize data members
// Must be public, name is the same as the class, must have no return type
// Helps customize objects when being created

// a class can have other classes as members
// string name; like this

int main()
{
	system("pause");
	return 0;
}